package leetcode

func FindTargetSumWays(nums []int, target int) int {
	// 分成两组， 其中一个比另外一个多 target
	// 或者首先拿出 target 剩下的平分
	sum1 := 0 //
	sum2 := 0 //
	for i := 0; i < len(nums); i++ {

	}

	return 0
}
